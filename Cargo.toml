[package]
name = "deltaspace"
version = "0.1.1"
edition = "2024"
authors = ["Jarmoco"]

[dependencies]
libc = "0.2"


[profile.release]
strip = true
opt-level = "z"
codegen-units = 1
panic = "abort"
lto = true